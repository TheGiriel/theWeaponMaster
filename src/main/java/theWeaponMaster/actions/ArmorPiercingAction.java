
package theWeaponMaster.actions;

import com.megacrit.cardcrawl.actions.AbstractGameAction;

public class ArmorPiercingAction extends AbstractGameAction {

    /*public ArmorPiercingAction(AbstractPlayer p, AbstractMonster m, int damage) {
        int initialBlock = m.currentBlock;
        int armorPiercing = 0;
        AbstractDungeon.actionManager.addToBottom(new DamageAction(m, new DamageInfo(p, damage, DamageInfo.DamageType.NORMAL), Explo));
        if (m.hasPower(ManaBurnPower.POWER_ID)) {
            AbstractDungeon.actionManager.addToBottom(new LoseHPAction(m, AbstractDungeon.player, damage, AbstractGameAction.AttackEffect.FIRE));
        }
    }

*/
    @Override
    public void update() {

    }
}
